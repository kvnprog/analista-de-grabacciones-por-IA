server {
    listen 80;
    server_name _;                 # accept any hostname
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback + directory index fix
    location / {
        try_files $uri $uri/index.html /index.html;
    }
}
